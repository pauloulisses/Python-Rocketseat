# 🧠 MAPA MENTAL — LÓGICA DE PROGRAMAÇÃO EM PYTHON

## 🎯 OBJETIVO

**Receber dados → Processar → Decidir → Repetir → Mostrar resultado**

---

## 🖨️ SAÍDA DE DADOS

### `print()`

* Mostra informações na tela
* Usado para resultados, mensagens e testes

Ex.: `print("Olá")`

---

## ⌨️ ENTRADA DE DADOS

### `input()`

* Recebe dados do usuário
* **Sempre retorna texto (string)**

Ex.: `nome = input("Digite seu nome: ")`

---

## 🔢 TIPOS DE DADOS

### 🔸 Números

* `int` → inteiro (10, 20)
* `float` → decimal (1.75, 3.14)

Ex.: `idade = int(input())`

### 🔸 Boolean

* `True` / `False`
* Base das decisões

---

## 📦 ESTRUTURAS DE DADOS

### 📌 Lista `[]`

* Ordenada
* **Mutável** (pode mudar)
* Guarda vários valores

Ex.: `[1, 2, 3]`

### 📌 Tupla `()`

* Ordenada
* **Imutável** (não muda)
* Mais segura

Ex.: `(1, 2, 3)`

### 📌 Dicionário `{}`

* `chave : valor`
* Dados com significado

Ex.: `{ "nome": "Ana", "idade": 25 }`

---

## 🔀 CONDICIONAIS (DECISÃO)

### `if / elif / else`

* Escolhe caminhos
* Usa comparações e boolean

Ex.:

```
if idade >= 18:
    print("Maior")
else:
    print("Menor")
```

---

## 🔁 LOOPS (REPETIÇÃO)

### `for`

* Repetição controlada
* Percorre listas

### `while`

* Repete enquanto condição for verdadeira

---

## 🧠 FUNÇÕES (REUTILIZAÇÃO)

### `def`

* Bloco de código com nome
* Evita repetição
* Organiza o programa

Ex.:

```
def soma(a, b):
    return a + b
```

---

## ⚖️ OPERADORES

### Comparação

* `==` igual
* `!=` diferente
* `>` `<` `>=` `<=`

### Lógicos

* `and` (e)
* `or` (ou)
* `not` (não)

---

## 🧩 FLUXO MENTAL DO PROGRAMADOR

1. `input`
2. Converter (`int`, `float`)
3. Decidir (`if`)
4. Repetir (`for` / `while`)
5. Armazenar (`lista`, `dict`)
6. Reutilizar (`função`)
7. Mostrar (`print`)

---

## 🧠 RESUMO VISUAL RÁPIDO

* `print` → saída
* `input` → entrada
* `int/float` → números
* `bool` → decisão
* `[]` → lista
* `()` → tupla
* `{}` → dicionário
* `def` → função
* `if` → escolha
* `for/while` → repetição


